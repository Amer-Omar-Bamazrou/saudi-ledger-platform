{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "SESSION_SECRET",
      "description": "Session-cookie signing key (min 32 chars). Auto-generated for development; replace with a strong secret for production.",
      "requiredAtBoot": true,
      "generatedForDevelopment": true
    }
  ]
}
